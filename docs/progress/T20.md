### T20 — documents + document_chunks migrations

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ viết xong (schema_documents.rs: 2 tests xác nhận documents + document_chunks có đủ columns, qdrant_point_id là UUID)
* [x] Test đỏ FAIL confirm (bảng chưa tồn tại trước migration)
* [x] Implement xong (DDL documents + document_chunks, GRANT gmrag_app)
* [x] Test xanh PASS confirm (2/2 schema_documents tests pass; clippy clean)
* [x] Commit: {pending}

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/migrations/20260617143700_documents.sql` | Tạo | DDL documents (owner_id, visibility, share_token, s3_key) + document_chunks (qdrant_point_id UUID) |
| `backend/crates/api/tests/schema_documents.rs` | Tạo | 2 schema verification tests |

#### Env vars / Config mới thêm (nếu có)
N/A

#### Migration version hiện tại (nếu có chạy migration)
`20260617143700_documents.sql` — Applied

#### Quyết định kỹ thuật
- **qdrant_point_id là UUID:** Vector embedding lưu ở Qdrant; PG chỉ giữ point reference (UUID) để join metadata với vector. Tuân thủ nguyên tắc "Vector lưu ở Qdrant; PostgreSQL chỉ giữ metadata".
- **workspace_id nullable:** Document có thể thuộc workspace hoặc standalone (tenant-scoped).
- **share_token UUID nullable:** Cho phép share document qua token public (visibility = 'shared').
- **status default 'uploaded':** Lifecycle: uploaded → processing → ready → failed.
- **tenant_id trên document_chunks:** Denormalized để RLS policy uniform `tenant_id = gmrag_current_tenant()` áp dụng đồng nhất (giống workspace_members ở T19).

#### Blocker / Warning cho batch tiếp theo
N/A — task schema-only, không blocker.

#### Tasks được unblock bởi task này
* T20 → unblock: T25 (RLS policy sẽ cover documents + document_chunks)
