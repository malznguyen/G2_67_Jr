### T21 — graph_nodes and graph_edges migrations

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ viết xong (schema_graph.rs: 2 tests xác nhận graph_nodes + graph_edges có đủ columns, properties là JSONB, UNIQUE(src,dst,kind))
* [x] Test đỏ FAIL confirm (bảng chưa tồn tại trước migration)
* [x] Implement xong (DDL graph_nodes + graph_edges, GRANT gmrag_app)
* [x] Test xanh PASS confirm (2/2 schema_graph tests pass; clippy clean)
* [x] Commit: {pending}

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/migrations/20260617143822_graph_entities.sql` | Tạo | DDL graph_nodes (kind, label, properties JSONB) + graph_edges (src/dst, kind, weight, UNIQUE) |
| `backend/crates/api/tests/schema_graph.rs` | Tạo | 2 schema verification tests |

#### Env vars / Config mới thêm (nếu có)
N/A

#### Migration version hiện tại (nếu có chạy migration)
`20260617143822_graph_entities.sql` — Applied

#### Quyết định kỹ thuật
- **properties JSONB:** Lưu metadata tùy biến cho node/edge (vd: entity type, confidence score, source span). sqlx `json` feature đã enable ở workspace Cargo.toml.
- **UNIQUE(src_node_id, dst_node_id, kind):** Tránh edge trùng lặp cùng loại giữa 2 node. Cho phép nhiều edge khác kind giữa cùng cặp node.
- **weight REAL default 1.0:** Trọng số relationship cho graph algorithms.
- **tenant_id trên cả 2 bảng:** Denormalized cho RLS uniform policy (T25).

#### Blocker / Warning cho batch tiếp theo
N/A — task schema-only.

#### Tasks được unblock bởi task này
* T21 → unblock: T25 (RLS policy sẽ cover graph_nodes + graph_edges)
