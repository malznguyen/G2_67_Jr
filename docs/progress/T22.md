### T22 — chat_sessions and chat_messages migrations

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ viết xong (schema_chat.rs: 2 tests xác nhận chat_sessions + chat_messages columns, FK cascade functional test)
* [x] Test đỏ FAIL confirm (bảng chưa tồn tại trước migration; cascade test ban đầu fail do confdeltype char mapping → chuyển sang functional test)
* [x] Implement xong (DDL chat_sessions + chat_messages, GRANT gmrag_app)
* [x] Test xanh PASS confirm (2/2 schema_chat tests pass; clippy clean)
* [x] Commit: {pending}

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/migrations/20260617144046_chat.sql` | Tạo | DDL chat_sessions (workspace_id nullable, model) + chat_messages (role, content, token_count, cascade) |
| `backend/crates/api/tests/schema_chat.rs` | Tạo | 2 schema verification tests (columns + cascade functional test) |

#### Env vars / Config mới thêm (nếu có)
N/A

#### Migration version hiện tại (nếu có chạy migration)
`20260617144046_chat.sql` — Applied

#### Quyết định kỹ thuật
- **role TEXT (không enum):** Giá trị 'user'/'assistant'/'system'. Tránh thêm enum type phức tạp ở giai đoạn schema; validate ở application layer.
- **workspace_id ON DELETE SET NULL:** Xóa workspace không xóa chat history — session giữ lại nhưng workspace_id = NULL. Khác với document_chunks (CASCADE) vì chat session có thể tồn tại độc lập.
- **token_count nullable:** Không phải message nào đều có token count (vd: system prompt, error messages).
- **Cascade test functional:** Thay vì check `pg_constraint.confdeltype` (char type map lỗi trong sqlx), test insert→delete→count để verify cascade behavior thực tế.

#### Blocker / Warning cho batch tiếp theo
N/A — task schema-only.

#### Tasks được unblock bởi task này
* T22 → unblock: T25 (RLS policy sẽ cover chat_sessions + chat_messages)
