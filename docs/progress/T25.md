### T25 — apply rls policies to domain tables and isolation tests

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ viết xong (8 new cross-tenant isolation tests: workspaces, documents, chat_sessions, audit_log, ingest_jobs, graph_nodes, invitations, no-context-hides-all)
* [x] Test đỏ FAIL confirm (8 tests fail: `left: 2, right: 1` — data leak vì chưa có RLS policy)
* [x] Implement xong (rls_apply_all migration: ENABLE + FORCE + CREATE POLICY trên 14 bảng mới)
* [x] Test xanh PASS confirm (13/13 rls_isolation tests pass; 78/78 workspace total; clippy clean)
* [x] Commit: {pending}

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/migrations/20260617145935_rls_apply_all.sql` | Tạo | ENABLE + FORCE RLS + CREATE POLICY `tenant_id = gmrag_current_tenant()` trên 14 bảng (T19-T24) |
| `backend/crates/api/tests/rls_isolation.rs` | Sửa | Thêm 8 cross-tenant isolation tests + 1 no-context test cho 14 bảng; diagnostic assertions (policy exists, RLS enabled, role switch) |

#### Env vars / Config mới thêm (nếu có)
N/A

#### Migration version hiện tại (nếu có chạy migration)
`20260617145935_rls_apply_all.sql` — Applied

#### Quyết định kỹ thuật
- **Chỉ thêm policy cho 14 bảng mới (R4 decision):** Không động tenants/tenant_members đã có RLS từ T12/T15. Tránh DROP/CREATE policy rủi ro trên migration cũ.
- **FORCE ROW LEVEL SECURITY trên tất cả 14 bảng:** Đảm bảo table owner cũng bị RLS. Runtime enforcement phụ thuộc `init_app_pool` (T19) downgrades → `gmrag_app`. Tests dùng `SET LOCAL ROLE gmrag_app` theo pattern T15.
- **Policy uniform `tenant_id = gmrag_current_tenant()`:** Tất cả 14 bảng đều có cột `tenant_id` (denormalized ở workspace_members, document_chunks, chat_messages, graph_edges, resource_acl, invitations, usage_events, audit_log, ingest_jobs) để policy đồng nhất.
- **Giải quyết stale test DB (Ghi nhận quan trọng):** `#[sqlx::test]` tái sử dụng test database từ lần chạy trước. Test DB tạo ở RED run (trước rls_apply_all) không có policy → tests vẫn fail dù migration đã apply. Fix: DROP tất cả `_sqlx_test_%` databases → `#[sqlx::test]` tạo fresh DB với đầy đủ migrations. Batch tiếp theo cần DROP test DBs khi thêm migration mới, hoặc dùng `cargo sqlx migrate run` trong test setup.
- **Diagnostic assertions trong rls_workspaces test:** Giữ lại assertions kiểm tra policy exists + RLS enabled + role switch — useful regression guard.

#### Blocker / Warning cho batch tiếp theo
⚠️ `#[sqlx::test]` cache test databases — khi thêm migration mới (T26 seed hoặc batch sau), cần DROP `_sqlx_test_%` databases trước khi chạy test, hoặc sqlx sẽ dùng DB cũ thiếu migration mới.
⚠️ HTTP-level RLS path vẫn chưa test end-to-end (BLOCKER-3 carry): `SharedConnection` chưa được consume bởi handler tenant-scoped thật. T25 test DB-level isolation only.

#### Tasks được unblock bởi task này
* T25 → unblock: T26 (seed data sẽ chạy trên DB có đầy đủ RLS), future batches (tenant-scoped handlers có thể yên tâm RLS đã enforce)
