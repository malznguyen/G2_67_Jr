### T29 — Collection schema: graph_{tenant_id} dim 768 + payload {node_id, workspace_id, entity_name}

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ viết xong (`create_graph_collection_creates_with_payload_indexes` — verify collection tồn tại + 3 payload indexes đúng type + vector 768/Cosine + delete cleanup)
* [x] Test đỏ FAIL confirm (1 error: `no method named create_graph_collection` found for `QdrantStore`)
* [x] Implement xong (`create_graph_collection` — reuse private helper `create_collection_with_indexes` từ T28 với 3 indexes)
* [x] Test xanh PASS confirm (12/12 gmrag-core tests; clippy `-D warnings` core clean; workspace clippy clean với `SQLX_OFFLINE=true`)
* [x] Commit: `deb1d83`

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/crates/core/src/qdrant/store.rs` | Sửa | Thêm `create_graph_collection(tenant_id)` — reuse `create_collection_with_indexes` (T28 helper) với 3 payload indexes (`node_id` Uuid, `workspace_id` Uuid, `entity_name` Keyword). Test `create_graph_collection_creates_with_payload_indexes`. |

#### Env vars / Config mới thêm (nếu có)
N/A

#### Migration version hiện tại (nếu có chạy migration)
N/A — T29 không chạm PostgreSQL.

#### Quyết định kỹ thuật
- **Collection naming `graph_{tenant_id}`:** Theo spec — tenant-isolated, song song với `chunks_{tenant_id}` (T28). Cùng tenant sẽ có 2 collection độc lập: 1 cho document chunks (RAG), 1 cho graph nodes (GraphRAG).
- **Reuse `create_collection_with_indexes` (T28 helper):** T29 không viết lại logic create-collection + create-index. Helper nhận `&[(&str, FieldType)]` → T29 chỉ truyền 3 entries. Giảm duplication, đảm bảo vector config (768/Cosine/HNSW-default) nhất quán giữa chunks + graph.
- **Payload index mapping:**
  - `node_id`      → `FieldType::Uuid` (PK của graph node, lookup trực tiếp)
  - `workspace_id` → `FieldType::Uuid` (filter theo workspace trong tenant)
  - `entity_name`  → `FieldType::Keyword` (filter exact-match theo tên entity)
- **Vector config:** Giống T28 — single vector 768/Cosine, Qdrant-default HNSW. Verify test: `Config::Params(vp)`, `vp.size == 768`, `vp.distance() == Distance::Cosine`.
- **`.wait(true)` trên index creation:** Giống T28 — đảm bảo index materialize trước khi return.
- **Test verify pattern:** Giống T28 — `collection_info` → `payload_schema` check 3 fields + vector config check. Reuse `payload_index_type` helper + `local_store` từ T28. Cleanup-first + cleanup-at-end.

#### Blocker / Warning cho batch tiếp theo
⚠️ QUAN TRỌNG
* **`create_graph_collection` không idempotent:** Giống T28 — gọi 2 lần với cùng `tenant_id` fail. Caller cần check `list_collection_names` trước hoặc dùng ensure-wrapper (future task).
* **Workspace clippy cần `SQLX_OFFLINE=true`** (pre-existing, không phải T29 gây ra): `gmrag-api` có `sqlx::query_as!` macro cần DB sống hoặc `.sqlx` offline cache. Từ host Windows, `DATABASE_URL` trỏ `postgres16` (không resolve) → macro fail `No such host is known`. Fix: set `SQLX_OFFLINE=true` (T26 đã sinh `.sqlx` cache) → workspace clippy clean. T29 chỉ chạm `gmrag-core` → không regression.
* **GraphRAG node upsert/search chưa implement:** T29 chỉ tạo collection schema. Việc upsert graph nodes (embed entity description → vector + payload) + search (kNN theo workspace_id filter) thuộc task sau (vd: worker GraphRAG pipeline). Cần payload `node_id` để join với `graph_nodes` table trong PostgreSQL (T21 migration đã có `qdrant_point_id` column pattern).
* **HNSW default (không explicit `m`/`ef_construct`):** Giống T28. Production tuning thuộc task perf riêng.

#### Tasks được unblock bởi task này
* T29 → unblock: future graph node upsert/search tasks, worker GraphRAG ingestion pipeline (provision graph collection per tenant + embed entity nodes), API graph query endpoints (kNN search trên `graph_{tenant_id}` filter theo `workspace_id`).
