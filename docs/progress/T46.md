### T46 — API chunk retrieval: embed query → Qdrant search top-5 (ACL filter)

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Thêm `api/src/chat/retrieval.rs` với `accessible_document_ids`, `retrieve_chunks`, `retrieve_chunks_with_vector`.
* [x] ACL pre-filter PostgreSQL: workspace member check + documents readable qua `visibility` / `owner_id` / `resource_acl`.
* [x] Qdrant filter: `must workspace_id` + `should document_id` (min_should=1), `top_k=5` default.
* [x] Hydrate `document_chunks.content` + `documents.title` từ `qdrant_point_id`; post-filter ACL; gán `citation_index` 1..N.
* [x] Wire `QdrantStore` vào API bootstrap (`lib.rs` Extension) + refactor `main.rs` → `gmrag_api::run()`.
* [x] Tests: `accessible_docs_excludes_private_foreign_owner`, `retrieve_chunks_empty_when_no_accessible_docs`, `retrieve_chunks_returns_top_scored_accessible` (`#[sqlx::test]` + Qdrant integration).

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
|------|-----------|---------|
| `backend/crates/api/src/chat/retrieval.rs` | Tạo mới | T46 chunk retrieval + ACL. |
| `backend/crates/api/src/chat/mod.rs` | Tạo mới | Re-export retrieval types. |
| `backend/crates/api/src/lib.rs` | Tạo mới | Library root + Qdrant init. |
| `backend/crates/api/src/main.rs` | Sửa | Thin binary wrapper. |
| `backend/crates/api/Cargo.toml` | Sửa | `[lib]`, `qdrant-client`. |

#### Quyết định kỹ thuật
- **ACL ở PG trước Qdrant**: `resource_acl` không có trong Qdrant payload → pre-filter `document_id` list, build `Filter::should` + `min_should=1`.
- **Embed qua `LlmProvider`**: Handler T49 gọi `resolve_llm_config` rồi `DeepSeekProvider::new` (T45 pattern).
- **`retrieve_chunks_with_vector`**: Cho phép T47/T49 embed một lần, reuse vector.

#### Verify
* `cargo test -p gmrag-api --lib -- chat::tests` → 4/4 prompt tests pass (T48 cùng module).
* `cargo test -p gmrag-api --lib -- chat::retrieval::tests::retrieve_all_embeds_once` → pass.
* `cargo clippy -p gmrag-api --lib -- -D warnings` → pass.
* `#[sqlx::test]` retrieval tests cần Postgres + Qdrant (`localhost:6334`) — chạy trong stack Docker dev.

#### Blocker / Warning cho task tiếp theo
* T47 mở rộng cùng file `retrieval.rs`.
* Chưa có HTTP route; T49 mount chat SSE sau khi có `assemble_system_prompt` (T48).
