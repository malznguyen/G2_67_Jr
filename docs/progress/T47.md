### T47 — Graph retrieval: Qdrant nodes top-5 + ILIKE fallback + edges

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] `retrieve_graph_context`: `search_graph_nodes` top-5, hydrate `graph_nodes` từ PG.
* [x] ILIKE fallback khi vector hits rỗng hoặc score < `GRAPH_SCORE_THRESHOLD` (0.25) trên `label` / `properties.description`.
* [x] `load_graph_edges`: edges có endpoint trong hit set, join labels.
* [x] `retrieve_all` / `retrieve_all_with_provider`: embed một lần → chunks rồi graph (sequential, cùng `PgConnection`).
* [x] Tests: `graph_ilike_fallback_finds_label_match`, `graph_edges_connect_retrieved_nodes`, `retrieve_all_embeds_once`.

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
|------|-----------|---------|
| `backend/crates/api/src/chat/retrieval.rs` | Sửa | T47 graph + orchestrator. |
| `backend/crates/api/src/chat/mod.rs` | Sửa | Re-export `retrieve_all_with_provider`, `GraphContext`. |

#### Quyết định kỹ thuật
- **Reuse query vector từ T46**: Không gọi `embed_query` lần hai trong `retrieve_all_with_provider`.
- **ILIKE `%query%`**: Đơn giản, LIMIT `top_k`; dedupe theo `node_id` với vector hits.
- **Edges MVP**: Load edges touch bất kỳ retrieved node; cả hai endpoint labels trong output.

#### Verify
* Unit/orchestrator tests pass trên host không DB.
* Graph integration tests (`#[sqlx::test]`) cần Postgres + Qdrant dev stack.

#### Blocker / Warning cho task tiếp theo
* T48 `assemble_system_prompt` nhận `GraphContext` + `ChunkHit` slices.
* Parallel `tokio::join!` chunks+graph cần hai RLS connections — defer đến khi có pool borrow helper.
