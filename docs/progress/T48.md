### T48 — `assemble_system_prompt` (port + `[chunk:N]` citations)

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Pure fn `assemble_system_prompt(chunks, graph) -> String` trong `api/src/chat/mod.rs`.
* [x] Template: GMRAG instructions + `## Document excerpts` với `[chunk:N] (source: …)` + optional `## Knowledge graph` (entities + relationships).
* [x] `N` = `ChunkHit.citation_index` (1-based) — căn T50 `resolve_chunk_index_citations`.
* [x] Unit tests: chunk tags order, omit empty graph, entities/edges, zero chunks message.

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
|------|-----------|---------|
| `backend/crates/api/src/chat/mod.rs` | Sửa | `assemble_system_prompt` + 4 unit tests. |

#### Quyết định kỹ thuật
- **Pure function, no I/O**: T49 gọi sau `retrieve_all_with_provider`.
- **Source label**: Ưu tiên `filename` (document title từ PG), fallback `document_id`.
- **Graph section omitted** khi `graph.nodes` rỗng.

#### Verify
* `cargo test -p gmrag-api --lib -- chat::tests` → 4/4 pass.
* `cargo clippy -p gmrag-api --lib -- -D warnings` → pass.

#### Blocker / Warning cho task tiếp theo
* T49: `ChatMessage::new("system", assemble_system_prompt(...))` + user query → `LlmProvider::chat_stream`.
* T50: parse `[chunk:N]` trong SSE stream, map `N` → `ChunkHit.point_id`.
