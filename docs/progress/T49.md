### T49 — `streaming.rs` DeepSeek SSE stream + DeepseekTokenParser port

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Thêm `api/src/chat/streaming.rs` với `DeepseekTokenParser` (buffer + holdback cho `[chunk:N]` bị cắt delta).
* [x] `ChatStreamEvent`: `Text`, `Citation { index }`, `Done { finish_reason }`.
* [x] `stream_rag_response`: `assemble_system_prompt` + user query → `LlmProvider::chat_stream` → parsed events.
* [x] Helpers: `collect_stream_events`, `assistant_text_from_events`, `meter_rag_chat_completion` (T51 hook).
* [x] Tests: parser biên (split tag, complete tag, finish holdback), mock provider, wiremock SSE end-to-end.

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
|------|-----------|---------|
| `backend/crates/api/src/chat/streaming.rs` | Tạo mới | T49 parser + orchestrator + tests. |
| `backend/crates/api/src/chat/mod.rs` | Sửa | `pub mod streaming`, re-export types. |

#### Quyết định kỹ thuật
- **Không Axum SSE ở T49**: Chỉ stream Rust nội bộ; T61 map sang HTTP SSE.
- **Holdback suffix**: Giữ partial `[chunk:` ở cuối buffer để tránh emit text sai khi delta cắt tag (R8).
- **Metering sau stream**: `meter_rag_chat_completion` ghi usage sau khi consumer thu events (tránh borrow `PgConnection` trong async stream).

#### Verify
* `cargo test -p gmrag-api --lib -- chat::streaming` → pass.
* `cargo clippy -p gmrag-api --lib -- -D warnings` → pass.

#### Blocker / Warning cho task tiếp theo
* T61: map `ChatStreamEvent` / `EnrichedChatStreamEvent` → JSON SSE client (`token`, `citation`, `done`).
* T61 gọi `meter_rag_chat_completion` sau khi stream xong (cùng RLS transaction).
