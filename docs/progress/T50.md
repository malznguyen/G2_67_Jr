### T50 — `resolve_chunk_index_citations` (index → point_id)

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] `ResolvedCitation` struct: `index`, `point_id`, `document_id`, `chunk_index`, `filename`.
* [x] `resolve_citation(chunks, index) -> Option<ResolvedCitation>`.
* [x] `resolve_chunk_index_citations(chunks, indices)` — dedupe, skip unknown index.
* [x] `enrich_stream_events(chunks, events)` → `EnrichedChatStreamEvent` (`CitationResolved` / `CitationUnknown`).
* [x] Unit tests: map index, unknown skip, dedupe, empty chunks, enrich stream.

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
|------|-----------|---------|
| `backend/crates/api/src/chat/mod.rs` | Sửa | T50 resolver + enrich + 5 tests. |

#### Quyết định kỹ thuật
- **1-based index** khớp `ChunkHit.citation_index` và prompt T48 `[chunk:N]`.
- **Unknown index**: parser T49 vẫn emit `Citation { index }`; enrich/map bỏ qua hoặc `CitationUnknown` cho client.
- **Dedupe**: Iterator trùng index chỉ resolve lần đầu (phù hợp citation list trong UI).

#### Verify
* `cargo test -p gmrag-api --lib -- chat::tests` → pass (T48 + T50).
* `cargo clippy -p gmrag-api --lib -- -D warnings` → pass.

#### Blocker / Warning cho task tiếp theo
* T61 SSE payload citation nên dùng `ResolvedCitation` (point_id UUID) từ `enrich_stream_events`.
