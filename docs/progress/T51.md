### T51 — Metering: ghi `usage_events` (`llm_tokens`, `embedding_tokens`)

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Module `api/src/metering.rs`: `record_usage_event`, `record_embedding_usage`, `record_llm_usage`.
* [x] Token count MVP: `tiktoken-rs` cl100k (cùng family worker chunking).
* [x] Metrics: `embedding_tokens` (query embed), `llm_tokens` (input system+user + output assistant text).
* [x] Hook retrieval: `retrieve_all_with_metering` — embed once, ghi embed usage, rồi chunk/graph.
* [x] Hook chat: `meter_rag_chat_completion` sau stream (T49).
* [x] Integration tests `tests/metering.rs`: insert row + RLS tenant isolation (`#[sqlx::test]`).

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
|------|-----------|---------|
| `backend/crates/api/src/metering.rs` | Tạo mới | T51 core + unit tests. |
| `backend/crates/api/src/lib.rs` | Sửa | `pub mod metering`. |
| `backend/crates/api/src/chat/retrieval.rs` | Sửa | `retrieve_all_with_metering`, `RetrievalError::Metering`. |
| `backend/crates/api/src/chat/streaming.rs` | Sửa | `meter_rag_chat_completion`, `assistant_text_from_events`. |
| `backend/crates/api/tests/metering.rs` | Tạo mới | RLS integration tests. |
| `backend/crates/api/Cargo.toml` | Sửa | `tiktoken-rs`. |

#### Quyết định kỹ thuật
- **Append-only `usage_events`**: INSERT only, metadata JSONB `{ operation, model, input_tokens?, output_tokens? }`.
- **Không dùng provider usage field**: DeepSeek stream chưa parse usage; tiktoken estimate đủ cho MVP quota (T69).
- **RLS**: Ghi qua `PgConnection` đã `SET LOCAL app.tenant_id` (middleware pattern T25).

#### Verify
* `cargo test -p gmrag-api --lib -- metering::` → pass (unit).
* `cargo test -p gmrag-api --test metering` → cần Postgres dev stack.
* `cargo clippy -p gmrag-api --lib -- -D warnings` → pass.

#### Blocker / Warning cho task tiếp theo
* T61 handler gọi `retrieve_all_with_metering` + `meter_rag_chat_completion` trong cùng RLS tx.
* T69 GET usage aggregate từ `usage_events` — ngoài scope Sprint 6C.
