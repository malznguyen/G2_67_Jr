### T61 — routes/chat.rs POST chat (SSE) + session ACL + test

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ viết xong (`tests/chat_routes.rs`: SSE parse, citation, auth 404, cross-tenant)
* [x] Test đỏ FAIL confirm — route/handler chưa tồn tại
* [x] Implement xong (`routes/chat.rs` `post_chat`, SSE mapper `ChatSsePayload`, pipeline RAG)
* [x] Test xanh PASS confirm: `chat_routes` 7 passed (3 T61 + 4 T62 trong cùng file)
* [x] Clippy `-D warnings` (SQLX_OFFLINE=true) → ExitCode 0
* [x] Commit: 548ede1

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/crates/api/src/routes/chat.rs` | Tạo | `post_chat` SSE, `check_relation(chat_session, viewer)`, 3-phase RLS (pre-stream / stream / post-stream tx) |
| `backend/crates/api/src/chat/mod.rs` | Sửa | `ChatSsePayload` + mapping `EnrichedChatStreamEvent` |
| `backend/crates/api/src/middleware/rls.rs` | Sửa | `with_rls_connection` helper |
| `backend/crates/api/src/lib.rs` | Sửa | Wire chat routes, `LlmRuntime` extension |
| `backend/crates/api/tests/chat_routes.rs` | Tạo | Integration tests T61/T62 |
| `backend/.sqlx/*` | Không đổi | no-op — routes dùng runtime `sqlx::query`, không có `query!` macro (xem `AUDIT_PRE_FRONTEND.md` C4) |

#### Env vars / Config mới thêm (nếu có)
N/A — tái sử dụng `LlmRuntime` từ `Config` (deepseek/ollama/BYOK key).

#### Migration version hiện tại (nếu có chạy migration)
`20260622000000_rebac_relation_tuples`

#### Quyết định kỹ thuật
- **AuthZ:** `check_relation(ObjectRef::chat_session, Relation::Viewer, user)`; thiếu quyền → `404`.
- **SSE JSON:** `{"type":"text|citation|citation_unknown|done|error", ...}` qua `axum::response::sse::Event::json_data`.
- **RLS + streaming:** Phase A trên `SharedConnection` (insert user msg + retrieval metering); Phase B stream LLM; Phase C **tx mới** qua `AppPool` (middleware COMMIT trước khi body SSE kết thúc) cho `meter_rag_chat_completion` + assistant message.
- **Test helper:** `post_chat_sse_with_context` inject mock `LlmProvider` + chunks, tránh Qdrant trong test SSE.

#### Blocker / Warning cho batch tiếp theo
* Session không có `workspace_id` → bỏ qua retrieval (chat vẫn stream, không embed metering).
* Post-stream persist lỗi → SSE event `error`/`persist-failed` (client nên xử lý).

#### Tasks được unblock bởi task này
* T62 (CRUD sessions), T70+ FE chat UI, T84 mount share dialog trên chat detail.
