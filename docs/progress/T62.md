### T62 — routes/chat.rs sessions/messages CRUD + ACL + test

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ viết xong (`tests/chat_routes.rs`: list/create/delete + owner guard + cross-tenant)
* [x] Test đỏ FAIL confirm — handlers chưa tồn tại
* [x] Implement xong (`list_sessions`, `create_session`, `delete_session` trong `routes/chat.rs`)
* [x] Test xanh PASS confirm: 4 test T62 trong `chat_routes` passed
* [x] Full suite `cargo test -p gmrag-api` → PASS
* [x] Commit: 548ede1

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/crates/api/src/routes/chat.rs` | Sửa | CRUD handlers + list predicate biên dịch `viewer` |
| `backend/crates/api/src/routes/mod.rs` | Sửa | `pub mod chat` |
| `backend/crates/api/src/lib.rs` | Sửa | Wire `/chat_sessions` routes |
| `backend/crates/api/tests/chat_routes.rs` | Sửa | 4 test CRUD |

#### Env vars / Config mới thêm (nếu có)
N/A

#### Migration version hiện tại (nếu có chạy migration)
`20260622000000_rebac_relation_tuples`

#### Quyết định kỹ thuật
- **Create:** `user_id` = caller (owner column), không nhận từ client.
- **List:** predicate OR owner / workspace member / `resource_acl` (user + workspace group) — đồng bộ `rbac/model.rs` viewer rewrite.
- **Delete:** `check_relation(chat_session, owner)`; không phải owner → `403`; cross-tenant/missing → `404`.
- **Messages:** persist qua T61 chat stream (không expose message CRUD riêng trong batch này).

#### Blocker / Warning cho batch tiếp theo
* List predicate biên dịch tay — nếu đổi `rewrite_for(chat_session, viewer)` phải đồng bộ SQL list.

#### Tasks được unblock bởi task này
* T63 (graph), FE session list UI (T70+).
