### T63 — routes/graph.rs GET graph (ACL-filtered) + test

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ viết xong (`tests/graph_routes.rs`: nodes/edges, non-member 404, cross-tenant)
* [x] Test đỏ FAIL confirm — `routes/graph.rs` chưa tồn tại
* [x] Implement xong (`get_workspace_graph` + wire `lib.rs`)
* [x] Test xanh PASS confirm: `graph_routes` 3 passed
* [x] Full suite `cargo test -p gmrag-api` → PASS
* [x] Commit: 548ede1

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/crates/api/src/routes/graph.rs` | Tạo | GET workspace graph (nodes + edges) |
| `backend/crates/api/src/routes/mod.rs` | Sửa | `pub mod graph` |
| `backend/crates/api/src/lib.rs` | Sửa | Wire `/workspaces/:wid/graph` |
| `backend/crates/api/tests/graph_routes.rs` | Tạo | 3 integration test |

#### Env vars / Config mới thêm (nếu có)
N/A

#### Migration version hiện tại (nếu có chạy migration)
`20260622000000_rebac_relation_tuples`

#### Quyết định kỹ thuật
- **AuthZ:** `check_relation(workspace, member, user)` — namespace `workspace` không định nghĩa `viewer` trong ReBAC MVP; `member` là gate đúng cho tài nguyên scoped workspace.
- **Query edges:** JOIN `graph_nodes` src/dst cùng `workspace_id` (edges không có cột workspace).
- **404 policy:** workspace không tồn tại hoặc không phải member → `404`.

#### Blocker / Warning cho batch tiếp theo
* Graph GET trả toàn bộ nodes/edges — chưa paginate; FE nên lazy-load nếu graph lớn.

#### Tasks được unblock bởi task này
* T70+ FE graph panel, T83 hoàn thiện ReBAC cho graph read paths (engine đã gọi trực tiếp).
