### T65 — `rbac/model.rs` relation-tuple types + namespace config + userset-rewrite rules

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ/đơn vị viết kèm module (`src/rbac/model.rs` `#[cfg(test)]`: round-trip relation/principal, concentric, inheritance, undefined, termination)
* [x] Implement xong (`src/rbac/model.rs` + `src/rbac/mod.rs` + đăng ký `pub mod rbac` trong `lib.rs`)
* [x] Test xanh PASS confirm: `cargo test -p gmrag-api --lib rbac::` → 10 passed; 0 failed
* [x] Clippy `-D warnings` → ExitCode 0

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/crates/api/src/rbac/model.rs` | Tạo | `Relation`, `ObjectRef`, `Principal`, `ParentEdge`, `RewriteOp`, `rewrite_for()` + namespace const. |
| `backend/crates/api/src/rbac/mod.rs` | Tạo | Khai báo `model`/`check`, re-export. |
| `backend/crates/api/src/lib.rs` | Sửa | `pub mod rbac;`. |

#### Quyết định kỹ thuật
- **Tách policy khỏi storage (đúng Zanzibar)**: `model.rs` thuần (không I/O) khai báo namespace config + userset-rewrite; phần resolve `_this`/`tuple_to_userset` xuống Postgres nằm ở `check.rs` (T66).
- **Userset rewrites (MVP)**:
  - `document`/`chat_session`: `owner = _this`; `editor = _this ∪ computed(owner)`; `viewer = _this ∪ computed(editor) ∪ tuple_to_userset(workspace → member)`.
  - `workspace`: `member = _this`.
  - Concentric `viewer ⊇ editor ⊇ owner` mô tả bằng `ComputedUserset`; kế thừa folder/parent bằng `TupleToUserset{Workspace, Member}`.
- **Quan hệ không xác định trả rule rỗng** → không bao giờ thỏa (an toàn mặc định).
- **Thay framing "role hierarchy OWNER>ADMIN>EDITOR>VIEWER" cũ** bằng concentric relations.

#### Blocker / Warning cho batch tiếp theo
* `model.rs` chỉ khai báo luật; mọi truy vấn DB ở `check.rs`. Thêm namespace mới phải cập nhật cả `rewrite_for` lẫn adapter trong `check.rs`.

#### Tasks được unblock bởi task này
* T66 (`rbac/check.rs`).
