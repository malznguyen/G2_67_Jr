### T68 — routes/settings.rs BYOK settings CRUD (encrypt on write)

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ viết xong
* [x] Test đỏ FAIL confirm (module `routes::settings` chưa tồn tại)
* [x] Implement xong
* [x] Test xanh PASS confirm: `settings_routes` 6 passed; unit tests 3 passed
* [x] Commit: 12afb82

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/crates/api/src/routes/settings.rs` | Tạo | GET/PUT `/tenants/:tid/settings/llm`, AES-GCM encrypt on write, masked key on read |
| `backend/crates/api/tests/settings_routes.rs` | Tạo | 6 integration tests (403, unconfigured, encrypt/mask, missing enc key, RLS) |
| `backend/crates/api/src/routes/mod.rs` | Sửa | `pub mod settings` |
| `backend/crates/api/src/lib.rs` | Sửa | Wire settings routes in `tenant_scoped` |
| `backend/.sqlx/` | Không đổi | no-op — routes dùng runtime `sqlx::query`, không có `query!` macro (xem `AUDIT_PRE_FRONTEND.md` C4) |

#### Env vars / Config mới thêm (nếu có)
N/A — tái sử dụng `GMRAG_TENANT_KEY_ENCRYPTION_KEY` (T45).

#### Migration version hiện tại (nếu có chạy migration)
`20260622000000_rebac_relation_tuples` (không migration mới cho T68).

#### Quyết định kỹ thuật
- **Encrypt on write:** PUT gọi `encrypt_with_aad(api_key, key, tenant_id.as_bytes())`, lưu `api_key_ciphertext` + `api_key_nonce`, set `api_key = NULL`.
- **Mask on read:** GET decrypt in-memory (owner-only) chỉ để tạo `api_key_masked` (`sk-***` + 4 ký tự cuối); không trả raw key hay ciphertext.
- **Unconfigured:** GET trả 200 `{ configured: false }` thay vì 404.
- **Owner-only:** Cả GET và PUT dùng `require_owner` từ `routes/tenants.rs`.

#### Blocker / Warning cho batch tiếp theo
⚠️ QUAN TRỌNG
* Test integration cần `COMMIT` trên `SharedConnection` sau mutation để verify qua superuser pool (pattern T52/T53).
* Ollama provider PUT không bắt buộc `api_key`; encrypted columns = NULL (CHECK pair vẫn hợp lệ).

#### Tasks được unblock bởi task này
* T69 → metering endpoints (batch tiếp theo, độc lập).
* FE settings UI (T70+) có thể gọi GET/PUT BYOK config.
