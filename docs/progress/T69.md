### T69 — Metering endpoints (usage, quotas, audit log)

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ viết xong
* [x] Test đỏ FAIL confirm (module `routes::metering` chưa tồn tại)
* [x] Implement xong
* [x] Test xanh PASS confirm: `metering_routes` 7 passed
* [x] Commit: b375b20

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/crates/api/src/routes/metering.rs` | Tạo | GET usage aggregate, quotas, audit_logs (owner-only) |
| `backend/crates/api/tests/metering_routes.rs` | Tạo | 7 integration tests |
| `backend/crates/api/src/routes/mod.rs` | Sửa | `pub mod metering` |
| `backend/crates/api/src/lib.rs` | Sửa | Wire 3 metering routes |
| `backend/.sqlx/` | Không đổi | no-op — routes dùng runtime `sqlx::query`, không có `query!` macro (xem `docs/AUDIT_PRE_FRONTEND.md` C4) |

#### Env vars / Config mới thêm (nếu có)
N/A

#### Migration version hiện tại (nếu có chạy migration)
`20260622000000_rebac_relation_tuples` (không migration mới cho T69).

#### Quyết định kỹ thuật
- **Usage aggregate:** `SELECT metric, SUM(delta) FROM usage_events WHERE tenant_id = $1 GROUP BY metric` trên `SharedConnection` (RLS tự lọc).
- **Quotas:** Nếu không có row → trả schema defaults với `configured: false` (khớp `documents.rs` khi thiếu quota row).
- **Audit logs:** `ORDER BY created_at DESC LIMIT 100` — không pagination offset (MVP).
- **Owner-only:** Cả 3 endpoint dùng `require_owner`.

#### Blocker / Warning cho batch tiếp theo
⚠️ QUAN TRỌNG
* `routes/metering.rs` (HTTP read) tách khỏi `api/src/metering.rs` (write-side T51) — import qualified.
* Audit log read chưa filter theo action/resource; FE có thể cần query params sau.

#### Tasks được unblock bởi task này
* T70+ FE baseline có thể hiển thị usage/quota/audit cho tenant owner.
* Quota dashboard / billing UI (future).
