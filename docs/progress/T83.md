### T83 — Tích hợp `check_relation` vào read paths của documents (thay ACL SQL inline)

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ viết xong (`tests/documents_acl.rs`: list-shared-via-acl, list-shared-with-workspace-group, preview-acl-recipient, preview-denied)
* [x] Test đỏ FAIL confirm — 3/4 fail (tài liệu chia sẻ qua `resource_acl` chưa hiện ra)
* [x] Implement xong (sửa `routes/documents.rs`: `list_documents`, `preview_document`, `delete_document`)
* [x] Test xanh PASS confirm: `documents_acl` 4 passed; `document_routes` 19 passed (T57/T58/T59/T60 không regression)
* [x] Clippy `-D warnings` → ExitCode 0

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/crates/api/src/routes/documents.rs` | Sửa | `preview_document` + `delete_document` dùng `check_relation` (viewer/owner); `list_documents` mở rộng predicate thêm grant `resource_acl` (user trực tiếp + nhóm workspace). |
| `backend/crates/api/tests/documents_acl.rs` | Tạo | 4 regression test. |

#### Quyết định kỹ thuật
- **`preview_document`**: load row theo id (RLS) → gate bằng `check_relation(document, viewer, user)`; thiếu row hoặc bị từ chối đều `404` (không lộ tồn tại). Thay predicate visibility inline cũ.
- **`delete_document`**: guard owner đổi từ so cột `owner_id` sang `check_relation(document, owner, user)`; vẫn `404` khi row không thấy, `403` khi không phải owner.
- **`list_documents`**: giữ 1 truy vấn có index (tránh N+1 per-row Check) bằng cách thêm OR-clause `resource_acl` — đây là "dạng biên dịch tập hợp" của `check_relation(viewer)`. Bổ sung grant user trực tiếp + grant nhóm workspace, cộng visibility/owner/workspace-member cũ.
- **chat/graph routes**: T61–T63 (`routes/chat.rs`, `routes/graph.rs`) CHƯA tồn tại (chưa làm) → T83 chỉ tích hợp `documents.rs`; engine `rbac` đã sẵn để các route đó gọi `check_relation(chat_session, …)` khi land.

#### Blocker / Warning cho batch tiếp theo
* Khi T61–T63 (chat/graph) được làm, gọi `check_relation` thay vì viết ACL SQL mới.
* `list` dùng predicate biên dịch tay; nếu namespace config (`rbac/model.rs`) đổi luật viewer thì phải đồng bộ predicate này (đã chú thích trong code).

#### Tasks được unblock bởi task này
* T85 (E2E), tích hợp ACL cho chat/graph khi T61–T63 hoàn thành.
