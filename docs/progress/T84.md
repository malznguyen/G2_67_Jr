### T84 — Frontend `AclShareDialog` + `lib/acl.ts` wiring tới `routes/acl.rs`

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Implement `lib/acl.ts` (typed client: `listGrants`/`createGrant`/`revokeGrant` + `aclClient` + `AclError`)
* [x] Implement `components/AclShareDialog.tsx` (client component: list/add/revoke grant theo resource)
* [x] Type-check: `npx tsc --noEmit` (frontend) → ExitCode 0, không lỗi

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `frontend/lib/acl.ts` | Tạo | Client gọi `GET/POST /tenants/{tid}/acl`, `DELETE /tenants/{tid}/acl/{grant_id}`; gửi `Authorization: Bearer` + `X-Tenant-Id`; parse envelope lỗi `{error:{code,message}}`. |
| `frontend/components/AclShareDialog.tsx` | Tạo | Dialog chia sẻ file/chat: chọn principal (`user`/`workspace`) + relation (`viewer`/`editor`), list người có quyền, nút Revoke. |

#### Quyết định kỹ thuật
- **Khớp hợp đồng backend T67**: kiểu `ShareableResource = document|chat_session`, `GrantableRelation = viewer|editor`, `PrincipalType = user|workspace`. Body POST đúng field snake_case (`resource_type`, `principal_id`, …).
- **Stateless/framework-agnostic** `lib/acl.ts`: nhận `AclClientConfig {baseUrl, tenantId, token}` tường minh, không global ẩn → dễ test/tái dùng.
- **Degrade gracefully**: chỉ owner mới share/revoke; backend tái-enforce (owner-only) → lỗi `forbidden` hiện inline thay vì giả định ở client.
- **A11y**: `role="dialog"`, `aria-modal`, `aria-labelledby`, label cho mọi input/select; ẩn nút Revoke với grant `owner`.

#### Blocker / Warning cho batch tiếp theo
* Frontend baseline (T70+) chưa làm → component đứng độc lập, chưa gắn vào trang/list tài liệu thật; khi T70–T77 land, mount `AclShareDialog` từ nút "Share" trên file/chat và truyền `token` (NextAuth/Keycloak) + `tenantId` hiện hành.
* Chưa có test runner FE (chỉ `next lint`/`tsc`); test tương tác để T85/T78.

#### Tasks được unblock bởi task này
* Tích hợp UI Share vào màn hình tài liệu/chat (T75), T85 (E2E).
