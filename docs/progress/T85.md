### T85 — ReBAC E2E + negative pentest (mở rộng T78/T79)

#### Trạng thái: ✅ Hoàn thành (backend E2E); phần FE-E2E nối tiếp khi T78/T79 có harness

#### Đã làm
* [x] Test viết xong (`tests/rebac_e2e.rs`: 5 test chạy qua handler thật)
* [x] Test xanh PASS confirm: `rebac_e2e` 5 passed; 0 failed
* [x] Toàn bộ `cargo test -p gmrag-api`: 90 lib + acl_routes 7 + documents_acl 4 + rbac_check 10 + rebac_e2e 5 + schema_rebac 6 + các suite cũ = 0 failed
* [x] Clippy `-D warnings` → ExitCode 0

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/crates/api/tests/rebac_e2e.rs` | Tạo | 5 E2E/pentest qua `create_grant`/`revoke_grant`/`list_grants` + `preview_document`. |

#### Kịch bản phủ
- **`e2e_share_grants_access_then_revoke_denies`**: trước share preview `404` → owner share viewer → preview `200` → owner revoke → preview `404`.
- **`pentest_viewer_cannot_reshare`**: viewer được cấp quyền thử re-share cho bên thứ ba → `403` (chống leo thang).
- **`pentest_cross_tenant_share_is_forbidden`**: attacker tenant A cấp quyền lên tài liệu tenant B → `403`; preview cross-tenant → `404` (RLS ẩn).
- **`e2e_workspace_inheritance_grants_preview`**: member workspace preview tài liệu trong workspace → `200`; outsider → `404`.
- **`pentest_revoked_user_cannot_list_grants`**: user không có quyền xem không liệt kê được grant → `404`.

#### Quyết định kỹ thuật
- **Chạy thẳng handler trên `SharedConnection`** (1 transaction chưa commit/tenant) đúng như middleware → grant tạo qua API thấy ngay ở preview cùng connection.
- **Phủ đúng "share→access, revoke→denied, cross-tenant denied, workspace inheritance"** như plan; cộng thêm chống leo thang quyền + chống liệt kê grant.

#### Blocker / Warning cho batch tiếp theo
* **FE-E2E (Playwright) thuộc T78/T79**: chưa có harness E2E trình duyệt; khi T78/T79 dựng xong, mở rộng kịch bản UI share→access bằng `AclShareDialog`. Backend đã phủ ma trận bảo mật cốt lõi.

#### Tasks được unblock bởi task này
* T78/T79 (E2E/pentest tổng thể) — phần ReBAC đã có nền backend.
