### T9 — core config struct and env parsing TDD

#### Trạng thái: ✅ Hoàn thành

#### Đã làm
* [x] Test đỏ viết xong
* [x] Test đỏ FAIL confirm (config_env_matrix fail do dotenvy reload .env)
* [x] Implement xong (added from_process_env + all subsystem configs)
* [x] Test xanh PASS confirm (9/9 core tests, 10/10 workspace tests)
* [x] Commit: bd5af2e

#### Files đã tạo/sửa
| File | Hành động | Ghi chú |
| ------ | ------ | ------ |
| `backend/crates/core/src/config.rs` | Sửa | Added OidcConfig, QdrantConfig, S3Config, RedisConfig, OllamaConfig, DeepSeekConfig structs. Added `from_process_env()` to isolate tests from `.env` file. Fixed config_env_matrix test. Added 5 new test cases. |

#### Env vars / Config mới thêm (nếu có)
| Tên | Giá trị mẫu | Ghi chú |
| ------ | ------ | ------ |
| KEYCLOAK_ISSUER | http://keycloak:8080/realms/gmrag | Required |
| KEYCLOAK_CLIENT_ID | gmrag-backend | Required |
| KEYCLOAK_CLIENT_SECRET | secret | Required |
| KEYCLOAK_FRONTEND_CLIENT_ID | gmrag-frontend | Optional, default gmrag-frontend |
| QDRANT_URL | http://qdrant:6333 | Optional, default localhost:6333 |
| QDRANT_API_KEY | | Optional |
| QDRANT_COLLECTION_DEFAULT | gmrag_chunks | Optional |
| S3_ENDPOINT | http://minio:9000 | Required |
| S3_PUBLIC_ENDPOINT | http://localhost:9000 | Optional |
| S3_REGION | us-east-1 | Optional |
| S3_ACCESS_KEY | key | Required |
| S3_SECRET_KEY | secret | Required |
| S3_BUCKET | gmrag-uploads | Required |
| S3_FORCE_PATH_STYLE | true | Optional, default true |
| REDIS_URL | redis://redis:6379/0 | Optional, default localhost:6379/0 |
| OLLAMA_HOST | http://ollama:11434 | Optional, default localhost:11434 |
| OLLAMA_EMBED_MODEL | nomic-embed-text | Optional |
| OLLAMA_LLM_MODEL | llama3.1:8b | Optional |
| OLLAMA_KEEP_ALIVE | 30m | Optional |
| DEEPSEEK_API_KEY | | Optional |
| DEEPSEEK_BASE_URL | https://api.deepseek.com/v1 | Optional |
| DEEPSEEK_MODEL | deepseek-v4-flash | Optional |
| DEEPSEEK_TIMEOUT_S | 60 | Optional |

#### Migration version hiện tại (nếu có chạy migration)
N/A

#### Quyết định kỹ thuật
- Added `from_process_env()` as `pub(crate)` to allow test isolation from `.env` file. `from_env()` remains the public API that loads `.env` first.
- Each subsystem (OIDC, Qdrant, S3, Redis, Ollama, DeepSeek) gets its own config struct for future separation.
- Optional fields use `Option<String>` with `env::var().ok().filter(empty)` pattern.

#### Blocker / Warning cho batch tiếp theo
⚠️ `main.rs` in gmrag-api still uses the old `Config` field set. It needs updating when wiring the new subsystem configs into AppState (future tasks).
⚠️ `.env.example` already contains all the env vars listed above — no changes needed there.

#### Tasks được unblock bởi task này
* T10 → unblock: JWT validation needs OidcConfig (issuer, client_id)
* T11 → unblock: AuthUser extractor needs T10
* T12 → unblock: migrations don't depend on T9 directly
